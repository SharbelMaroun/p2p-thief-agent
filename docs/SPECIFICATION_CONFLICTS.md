# Specification Conflicts and Decisions

| ID | Issue | Status | Controlling treatment | Evidence / ADR |
|---|---|---|---|---|
| C-001 | One-game example/default versus six sub-games per official series | `RESOLVED` | Use fixed six; one-game values are examples only | Appendix F Table 18; `AF-018` |
| C-002 | “Five README components plus cross-link” versus “six components” | `RESOLVED` | Cross-link is component six | Chapter 9.4.2; Appendix E rule 42; `SR-008` |
| C-003 | Simulator tool names vary or are absent from the book | `OPEN` | Treat simulator names as candidates only | ADR-0001; `U-003` |
| C-004 | `rimesegal` aliases versus official `rmisegal` addresses | `RESOLVED` | Use only `rmisegal@gmail.com` and `rmisegal+uoh26finalgame@gmail.com` | Appendix F Table 20; `AF-020` |
| C-005 | Archived references cite Appendix G for GitHub submission | `RESOLVED` | Current book v3.0.0 uses Appendix C | Official book Appendix C |
| C-006 | Claim that all four artifacts must be byte-identical | `RESOLVED` | Shared game config must be byte-identical; no evidence makes all role-specific artifacts identical | Appendix E rule 11; `AE-011`; `JS-002/003` |
| C-007 | Shared live state prohibition versus possible shared executable package | `OPEN` | No runtime dependency; use independent accepted file copies in M1 | `SR-004`; `U-010` |
| C-008 | Appendix B `schema_version` example `1.2` versus unauthenticated generated examples `1.1` | `OPEN` | Record both; do not normalize or freeze a supported version yet | PDF p.129; example hashes; ADR-0003 |
| C-009 | Book multiplicative scent decay versus simulator subtractive/immediate decay | `RESOLVED` | Book formula controls; simulator behavior must not be copied | Chapter 4.3, PDF p.43; pinned simulator export; ADR-0005 |
| C-010 | Branch claims LLM movement is categorically forbidden | `RESOLVED` | Deterministic movement is policy/default; rule 25 is a recommendation without automatic sanction | Appendix E rule 25; `AE-025`; ADR-0007 |
| C-011 | Documents claim Cop/Thief files are already byte-identical | `RESOLVED` | Make no parity claim until actual manifest comparison passes | Contract policy; `U-020` |
| C-012 | Generated template prose and simulator v3.0.0 `role_for()` alternate roles, while the book confirms six sub-games but does not define that schedule | `RESOLVED` | The coordinator relayed an authoritative lecturer answer on 2026-07-29: sub-games 1, 3, 5 natural role, 2, 4, 6 swapped, **Thief moves first**. `U-021` is closed on that authority. Provenance is a coordinator-relayed lecturer answer, not a Moodle announcement, and is recorded at that level. This row previously read `OPEN`, contradicting the closure already recorded in `UNKNOWN_REQUIREMENTS.md` | `AF-018`; `U-021` closed 2026-07-29; simulator `960499f` |
| C-013 | Appendix B links sorted-key canonical JSON to `config_sha256`, Chapter 5 gives compact UTF-8 bytes for a core commitment, and simulator v3.0.0 additionally uses unescaped Unicode | `RESOLVED` | Resolved on 2026-07-29 by adopting the simulator serialization repo-wide: one `canonical_json` with `sort_keys=True`, `ensure_ascii=False`, and compact separators serves the commitment, the agreed config, and audits. This reverses the 2026-07-28 ADR-0006 "keep book" ruling, which is superseded and archived. Disclosed under the book p. 5 contradiction clause | `CR-001`; `U-002`; `SIM_WIRE_PROTOCOL.md`; superseded ADR-0006; simulator `960499f` |
| C-014 | Book p. 43 states that at `ρ = 0.10` the factor `(1-ρ)` means "the existing scent is **reduced by 90%**" | `RESOLVED / BOOK ERROR` | Arithmetic error in the source. `(1 - 0.10) = 0.9`, so each turn **retains** 90% and reduces by 10%. Implement the formula `max(0, (1-ρ)·τ + Δτ)`, never the prose | Chapter 4.3, PDF p.43; ADR-0005; `M6-001`/`M6-005` |
| C-015 | Book p. 46 states that raising `ρ` toward 1.0 would leave the board "**saturated** with scent" | `RESOLVED / BOOK ERROR` | Reversed. `ρ → 1.0` drives `(1-ρ) → 0`, so scent vanishes almost immediately; saturation is what `ρ → 0` would approach. Sensitivity work under `M9-006b` must use the correct direction | Chapter 4.4, PDF p.46 |
| C-016 | Book ch. 7 (p. 74) verifies a replay step as `SHA256("{nonce}\|{move}")`, but ch. 5 (p. 53) seals the canonical payload with the nonce | `RESOLVED / M8` | The two constructions cannot agree, so a replay viewer built literally from ch. 7 fails every commitment produced by ch. 5, and rule 19 makes any audit mismatch an automatic zero. The book itself footnotes p. 74 as simplified. Build `M8-002` from `protocol/crypto.py`, never from the ch. 7 sketch **Not a conflict; the book resolves it in its own voice (verified 2026-08-06, `M8-02d`).** `:1757` (p. 58/146) footnotes the p. 74 listing directly: "the sketch simplified the input for the sake of the illustration; in practice the signature covers all components of the step — Intent, Move, State and Nonce — as detailed in the protocol in Chapter 5". Chapter 7 names chapter 5 as normative, so there is nothing to escalate and nothing is disclosed. The reference simulator diverges from p. 74 identically — its `verify_record` in `src/police_thief/gui/replay_data.py` recomputes over the revealed payload with the nonce, not over a bare move string. Reclassified from CONFLICT: leaving it here would have asked the lecturer to settle a question the source already settles. Action is unchanged — build the replay verifier from this repository's own construction, never from the ch. 7 sketch. | PDF pp. 53, 74; `SIM_WIRE_PROTOCOL.md`; `AE-019`; PDF p. 58/146 (`:1757`) |
| C-017 | Appendix F table 15 sets `[Step Limit]` and `[Survival Threshold]` to the same value (35), which reads as leaving it undefined whether surviving *exactly* the threshold is a Thief win | `RESOLVED` | Chapter 3 table 2 (PDF p. 38) settles it: the survival row is the Thief surviving "the limit of valid moves" without capture, and table 15 makes the limit and the threshold the same number. The horizon test is therefore **inclusive** — completing the final step uncaptured is a Thief win, which is what `resolve_outcome` already did. Pinned by a boundary test; `U-022` closed | Chapter 3 Table 2, PDF p. 38; Appendix F Table 15; `U-022`; `M3-005` |
| C-018 | `[Number of Agents]` names two different parameters | `CONFIRMED DISTINCTION` | Appendix F table 13 row 2 means players in a match (2, Fixed); table 18 row 1 means games in a series against one opponent (6, Fixed). Same bracket token, different meaning, both Fixed. Never conflate them | Appendix F Tables 13, 18 |
| C-019 | Appendix F table 17 lists five scoring rows and omits the technical-loss value, which the config schema requires | `CONFIRMED` | The value is fixed at 0/0 by table 2 (p. 38) and rule 48. The parameters table is incomplete, not permissive; the omission is not freedom to choose | Appendix F Table 17; Table 2; `AE-048` |
| C-020 | The binding board size is 7×7, but illustrations use 10×10, 6×6, 5×5, and 3×3 | `CONFIRMED DISTINCTION` | Only Appendix F table 13 binds. Build the board at the negotiated size and size every matrix to the board; treat all figure dimensions as illustration | Appendix F Table 13; figures 1, 8, 9 |
| C-021 | The same two appendices are cited under different letters throughout the book | `CONFIRMED DISTINCTION` | The mandatory-parameters table is called Appendix E, F, V, I, and "1" in different places, and its own heading on p. 151 reads "Appendix V"; the rules table is called Appendix D, E, and H. Resolve by content, not letter | PDF pp. 2, 4, 5, 10, 142, 151 |
| C-022 | The book's per-turn `Reveal` phase says the move is exchanged live; the pinned wire reference sends no move at all | `RESOLVED / SIM WIRE` | Chapter 5.3 and figure 6 define phase 3 as the peers exchanging their actual `Move` and `Hint` with only the `Nonce` withheld. Verified against the reference 2026-08-01: its live turn message carries only `step`, `sender`, `hint`, `smell_grid`, `commit`, `timestamp` and the optional claim members — the move, the true position, the bluff verdict, and the nonce stay private until the post-game audit, which is exactly what `protocol/wire.TurnMessage` already models. The wire reference governs here because interoperability is decided by what crosses the socket; nothing the book requires is lost, since the move is still revealed, only at the audit rather than mid-turn |
| C-023 | Chapter 4.4's case study names its quadrants upside down: it places the scent at `(1,4)`/`(1,3)` and calls that the **south-east** corner, then calls `(5,2)` a **northern** cell | `RESOLVED` | Trust the **intensities**, not the **cell labels**. Under the Appendix F table 13 origin — top-left, row growing downward — `(1,4)` is *north*-east and `(5,2)` is *southern*; north and south are swapped throughout the example. Resolved under the book's p. 5 contradiction clause: Appendix F outranks a chapter illustration, and the arithmetic (`0.81 = 0.9·(1−0.1)`, adjacent `0.63`, claimed direction `0.00`) is unaffected. **This repository's code is not affected** — `perception/trust.update_trust` measures agreement as the overlap `Σ hint·scent` against the uniform baseline and never transcribes the study's cells — but the finding is recorded here because both peers read the same chapter and the study *is* the specification for the lie-detection rule. Found 2026-08-06 in the companion repository, where a literal transcription made its own tests fail | Chapter 4.4 case study, `inst/police_thief_p2p_Summary.md:1007-1022`; Appendix F Table 13 |
| C-024 | This module rejected unknown fields on turn and audit messages, stating it "mirrors the simulator's own strictness per type" | `RESOLVED` | **The premise was wrong and the behaviour was harmful.** Asked directly on 2026-08-06, the reference **ignores** extra or unknown fields when building `TurnMessage`, `ControlMessage` *and* `AuditPayload`; the strictness it has is about **missing required** fields, which still raise. Worse, our rejection was silently fatal: a refused turn is consumed and skipped by the poller, so one unmodelled key made this peer go quiet until the deadline — indistinguishable from a dropped connection — and cost a technical loss. It also disagreed with the companion Cop, whose schema tolerates extras, so the same classmate message played against one peer and starved the other. Now ignored on all three types; missing required fields and invalid values still refused. "Strict in what we send, generous in what we accept" | Reference wire contract; the same rule already applied to acknowledgements |

No open conflict is resolved by selecting a simulator default, populated example, or
unaccepted proposal. Rows marked `BOOK ERROR` record an arithmetic mistake in the
source; they are disclosed under the book p. 5 contradiction clause and the corrected
reading is implemented.
