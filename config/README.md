# Configuration Status

Every file under `config/` is an unverified draft and must not be loaded by a runtime.

- `thief/` is `QUARANTINE` until the official schemas, Appendix F values, private fields,
  signing/agreement process, and local team choices are verified.
- `police/` is `REMOVE LATER`: it is role-confused material in a Thief repository.
  It is retained only to avoid deleting teammate work and must never be consumed here.

Values currently present—including versions, schema versions, grid/game counts, ports,
timeouts, rate limits, model/provider names, email addresses, modes, and filenames—are
examples or unsupported claims, not requirements. Do not edit them into a new schema.
